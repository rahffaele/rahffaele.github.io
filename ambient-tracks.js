const trackButton = document.getElementsByClassName("general-button");


  if (/* Your condition here */) {
    trackButton.classList.remove("general-button");
    trackButton.classList.add("disabled-button");
  } else {
    myButton.classList.remove("new-class");
    myButton.classList.add("original-class");
  }
